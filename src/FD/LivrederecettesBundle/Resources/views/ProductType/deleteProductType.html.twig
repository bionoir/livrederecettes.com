{# src/FD/LivrederecettesBundle/Resources/views/ProductType/deleteProductType.html.twig #}
{% extends "FDLivrederecettesBundle::layout.html.twig" %}
 
{% block title %}
    {{ parent() }} - Confirmer suppression du type de produit
{% endblock %}

{% block fdalivrederecettes_body %}

    <p> Voulez-vous vraiment supprimer le type de produit "{{ type.value }}" ? </p>
    
    <p>
        <form action="{{ path('livrederecettes_deleteProductType', {'id': productType.id}) }}" method="post">
            <a href="{{ path('livrederecettes_viewProductType', {'id': productType.id}) }}" class="btn btn-default">
                <i class="glyphicon glyphicon-chevron-left"></i>
                Retour à la fiche du type de produit
            </a>
            <input type="submit" value="Supprimer" class="btn btn-danger" />
            {{ form_rest(form) }}
        </form>
    </p>

{% endblock %}
