{# src/FD/LivrederecettesBundle/Resources/views/Product/viewProduct.html.twig #}
{% extends "FDLivrederecettesBundle::layout.html.twig" %}
 
{% block title %}
    {{ parent() }} - Détail d'un produit
{% endblock %}
 
{% block fdalivrederecettes_body %}
 
  <h4>{{ product.name }}</h4>
  
  <div class="well">
    Produit de type {{ product.productType.value }} dont la saison va du {{ product.seasonStart|date("j F") }} au {{ product.seasonEnd|date("j F") }}.
  </div>
  
  <p>
    <a href="{{ path('livrederecettes_listProducts') }}" class="btn btn-default btn-sm">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('livrederecettes_modifyProduct', {'id': product.id}) }}" class="btn btn-primary btn-sm">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier le produit
    </a>
    <a href="{{ path('livrederecettes_deleteProduct', {'id': product.id}) }}" class="btn btn-danger btn-sm">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer le produit
    </a>
  </p>
 
{% endblock %}
